<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit <%= design.title %></title>
</head>
<body>
  <h1>Edit <%= design.title %></h1>
<form action="/designs/<%= design._id %>?_method=PUT" method="POST">
    <label for="title">Title:</label>
    <input type="text" name="title" id="title" value="<%= design.title %>" />

    <label for="isPublished">Published:</label>
    <input
      type="checkbox"
      name="isPublished"
      id="isPublished"
      <% if (design.isPublished) { %> checked <% } %>
    />

    <button type="submit">Update Design</button>
  </form>

  <a href="/designs">Back to Designs</a>
</body>
</html>
